litt = [1,2,3,4,5,6]




import numpy as np

videoStartIndex = 0
videoLength = 2

for i in range(10):
    print(np.random.randint(videoStartIndex, videoStartIndex + videoLength))

num = np.random.randint(videoStartIndex, videoStartIndex + videoLength)